import React from "react";
import {Bar} from "react-chartjs-2";
import {Col} from "react-bootstrap";

function DailyTransactions() {
  return( 
  <Col>
  {/* <Bar data={{labels:['Red', 'Blue', 'Yellow']}} height={400} width={600}/> */}
    </Col>
  );
}

export default DailyTransactions;
